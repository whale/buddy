{
  "name": "buddy",
  "version": "0.4.31",
  "private": true,
  "license": "MIT",
  "description": "Buddy \u2014 a calm day companion. Web app (index.html) wrapped in a Tauri v2 Mac menu-bar shell.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "python3 src-tauri/gen-icons.py",
    "icons:mac": "bash scripts/icon-qa.sh",
    "icons:qa": "bash scripts/icon-qa.sh",
    "ui:smoke": "NODE_PATH=$(npm root -g) npx playwright test scripts/buddy-ui-smoke.spec.js --reporter=line",
    "test:merge": "NODE_PATH=$(npm root -g) npx playwright test scripts/buddy-merge.spec.js --reporter=line",
    "test:crossbuild": "NODE_PATH=$(npm root -g) npx playwright test scripts/buddy-crossbuild.spec.js --reporter=line",
    "sync:live": "NODE_PATH=$(npm root -g) npx playwright test scripts/buddy-two-device.spec.js --reporter=line",
    "sync:unlink": "NODE_PATH=$(npm root -g) npx playwright test scripts/buddy-unlink-live.spec.js --reporter=line",
    "sync:validate": "NODE_PATH=$(npm root -g) node scripts/buddy-sync-validate.mjs",
    "diag": "scripts/buddy-diag.sh",
    "sync:doctor": "node scripts/buddy-sync-doctor.mjs",
    "ios:beta": "bash scripts/ios-testflight.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
